# Repository-specific ignores

# ZSH specific
.zcompdump*
.zsh_history
.zsh_sessions/

# Local configuration files that shouldn't be committed
.gitconfig.local
.shell_local.sh
.credentials

# Certificate files (security - never commit certificates)
*.crt
*.pem
*.key
*.p12
*.pfx
